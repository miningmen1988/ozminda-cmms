{
  "name": "ozminda-cmms",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "android:prepare": "powershell.exe -ExecutionPolicy Bypass -File ./prepare-android-assets.ps1",
    "android:sync": "npm run android:prepare && npx cap sync android",
    "android:build": "npm run android:sync && cd android && gradlew.bat assembleDebug"
  },
  "devDependencies": {
    "@capacitor/android": "^8.3.4",
    "@capacitor/cli": "^8.3.4",
    "@capacitor/core": "^8.3.4"
  },
  "dependencies": {
    "pg": "^8.21.0"
  }
}
